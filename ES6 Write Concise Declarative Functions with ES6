const bicycle = {
  gear: 2,
  setGear(newGear) {
    this.gear = newGear;
  }
};

//Use class Syntax to Define a Constructor Function
class Vegetable {
  constructor(name) {
    this.name = name;
  }
}

const carrot = new Vegetable('carrot');

